[pytest]
markers =
    offline: marks tests that must run without network access (socket blocked)
    integration: marks tests requiring real external services (GOOGLE_API_KEY)
    ai: marks tests that make real LLM API calls (require GOOGLE_API_KEY)
